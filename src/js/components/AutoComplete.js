var React = require('react');
var AltContainer = require('alt/AltContainer');
var $ = require('jquery');  

var AutoComplete = React.createClass({ // creating a parent component that defines and alt container component

  render() {
    return (
      <div>
        <h6>Auto Complete Page</h6>
        
      </div>
    );
  }
});

module.exports = AutoComplete; //export the component